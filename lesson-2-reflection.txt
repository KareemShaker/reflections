What happens when you initialize a repository? Why do you need to do it?
it adds the .git hidden file to track the changes.
How is the staging area different from the working directory and the repository?
What value do you think it offers?
staging area is a middle phase between the working diretory and repository.
it is a step to allow you choose what to commit at his stage.

How can you use the staging area to make sure you have one commit per logical
change?
by adding only the file or files that has or have the changes 

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

if you are adding some new features which is not officialy approved or as suggestions.
branches would help that the new work will be in parallel with the main work 
but does not affect it
How do the diagrams help you visualize the branch structure?
it simplyfies how the branches and master looks like 

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
Gits can't merge if thereis a conflict in the file merging. 
So you need to resolve the conflict manually first then add and commite the change. 